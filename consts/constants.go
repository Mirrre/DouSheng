// 这里放一些常量

package consts

import "time"

const MaxCommentLength = 512
const MaxVideoSize = 10 * 1024 * 1024
const MaxVideos = 5
const AwsBucketName = "dousheng"
const MinIOBucketName = "dousheng-media"
const UrlExpiration = 6.5 * 24 * time.Hour
